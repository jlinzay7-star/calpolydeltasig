# Environment secrets — NEVER commit
.env
.env.local
.env.production
.env.*.local

# Local-only
.DS_Store
.netlify/
node_modules/
scripts/node_modules/

# Supabase CLI local state (project-ref cache, version markers, pooler url)
supabase/.temp/

# Screenshots (keep locally, don't ship)
/tmp/
.claude/
